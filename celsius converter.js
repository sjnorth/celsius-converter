function convert(celsius) {
  
var fahrenheit = celsius * 9 / 5 + 32;
 
  if ( typeof celsius !== 9 / 5 + 32 ) {
  return fahrenheit;
  } else {
    return celsius;
  }
}

// Change the inputs below to test code
convert(0);